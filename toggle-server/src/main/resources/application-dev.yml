spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:toggle_db}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

toggle:
  security:
    allow-local-callbacks: true
